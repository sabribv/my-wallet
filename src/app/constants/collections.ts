export enum collection {
  users = 'users',
  tokens = 'tokens',
  expenses = 'expenses',
  bills = 'bills',
  wallets = 'wallets',
  walletTransactions = 'transactions',
  exchanges = 'exchanges',
}
